sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t boltgun69/docker-react-demo -f Dockerfile.dev .

script:
  - docker run -e CI=true boltgun69/docker-react-demo npm run test
